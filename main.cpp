#include <iostream>
#include "algorithms/combinatorics.h"

int main()
{
    // Combinatoric thing
    //placements_with_repetitions(4, 7);
    //permutations_without_repetitions(6);
    //subsets_of_set(3, 5); // NOT READY
    //increasing_sequences(5, 7);
    positive_number_partitions(10); // READY, BUT WITH REPETITIONS
    return 0;
}
